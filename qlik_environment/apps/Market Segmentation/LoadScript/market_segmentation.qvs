Orders:
LOAD
    OrderID,
    OrderDate,
    CustomerID,
    EmployeeID,
    ShipperID,
    ProductID,
    Sales,
    Costs,
    Profit,
    Quantity,
    Discount,
    Freight
FROM [lib://qlik-data-store/example-data/QlikSenseWorkShop.xlsx]
(ooxml, embedded labels, table is Orders);
